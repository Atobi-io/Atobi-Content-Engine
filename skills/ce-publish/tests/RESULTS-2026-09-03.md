# Dry-run results — 2026-09-03 — ce-publish 0.1.0

Method: one fresh agent per fixture, given only SKILL.md, the two references and the
fixture's drop files (not expected.md). No MCP tools; local folder = dry-run path.
Operator answered "No" at every block-plan confirmation. No writes anywhere.

| Fixture | Expected | Result | Notes |
|---|---|---|---|
| 01-valid-journey | plan, stop at confirmation | PASS | Plan had 11 rows: caption and feedback each become a text block per Step 4. expected.md corrected (was 9 rows). |
| 02-valid-article | plan, 2 blocks, stop | PASS | Poll mapped to multi_choice, all false, required 0, not mandatory. |
| 03-no-review | gate stop | PASS | Stopped before parse; named review.md, ce-review, and the force override. |
| 04-review-fail | gate stop, 2 findings echoed | PASS | Both [High] first lines echoed; producer + re-review named. |
| 05-malformed-check | 2 format errors | PASS | Q breach reported as two entries (no [x]; 1 option). expected.md accepts either. |
| 06-extra-frontmatter | unknown field stop | PASS | "unknown frontmatter field: voice_audit", cites drop-format § Frontmatter. |
| 07-update-mode | id not found → create; plan as 02 | PASS | Offline path only; real update mode is exercised in the staging run. |

Skill wording fixed from agent ambiguity notes (all seven flagged the first):

1. Dry-run tenant — frontmatter has no tenant; Step 1 now uses `0` as placeholder.
2. `### Knowledge check` is a syntax marker, emits no heading block (Step 4).
3. `yes_no_task` has no answer key on Atobi; the `[x]` is the expected answer for
   reviewers only (Step 4 + drop-format table).
4. Pre-call checklist in Step 7 limited to payload-shape items; content-design items
   belong to the producer and ce-review.
5. Flat articles show `(none)` in the plan's Section column.
